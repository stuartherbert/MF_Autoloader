<?php

// this file should never be loaded by the autoloader;
// if it does get loaded, then the autoloader is in error

namespace MF\Underscore_Test;

class PSR0_UnderscoreData
{
        public $isLoaded = false;
}